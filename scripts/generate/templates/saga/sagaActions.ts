// Core
import { createAction } from '@reduxjs/toolkit';

// Slice
import { sliceName } from '../slice';

export const fetch__entityName__(pascalCase)Action = createAction<any>(`${sliceName}/FETCH___entityName__(constantCase)_ASYNC`);
