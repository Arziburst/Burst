// Types
import * as types from './types';

export const fetch__entityName__(pascalCase)ActionAsync: types.Fetch__entityName__(pascalCase)Contract = () => ({
    type: types.FETCH___entityName__(constantCase)_ASYNC,
});

